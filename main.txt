SRC-S:
  https://s3.amazonaws.com/cpvii/Training+materials/Unit+Eight+-+Ubuntu+Security.pdf
  
  https://www.youtube.com/watch?v=JVxkTqLoyGY
  https://www.youtube.com/watch?v=iOAMDEAgEWU
  
  http://lacapnm.org/Cadets/STEM/CyberPatriot/SeasonVIII/CyberPatriot_Linux_CheckList.pdf
  
////////////

Open Software Updater:
  Install update

dock < Softwate & updates < Updates:
  Import Security Updates (xenial-security) = True
  Recomended Updates (xenial-updates) = True
  Automaticly check for updates = Daily
  When there are security updates = Download and install automaticly
  
Open Terminal:
  $ sudo apt-get update && sudo apt-get upgrade

Go to firewall:
  Not Have:
    $ sudo apt install gufw
    $ gufw
  
  Profile = Home
  Status = ON
  Incoming = Reject (Refer to policy)
  Outgoing = Allow (Refer to policy)
  Add Special rules for apps if needed
  
  
Clamav:
  $ clamscan
  
Go To Users:
  Change Admin Privlage
  Change Passwords
  Delete un-needed passwords

Open Ubantu Software:
  Update All apps
  Remove un-needed apps
  
Open FireFox:
  Settings < Prefrences < Privacy & Security:
    Ask to save logins = False
    Show alerts = True
    FireFox will = Remember history
    Block Popups = True
    Warn when websites will try to install add-ons = True
    Block dangerous and deceptive content = True
    Block dangerous downloads = True
    Delete cookies when firefox is closed = True
    Browsing Policy = Strict
    
Open Terminal:
  cd etc/pam.d
  sudo apt install libpam-cracklib
  
  sudo nano common-password:
    add remember=5 to end of pam_unix.so
    add minlen=8 at the end of the line that has pam_unix.so
    add ucredit=-1 lcredit=-1 dcredit=-1 ocredit=-1 at the end of the line with pam_cracklib.so
    
    save
    
  sudo nano ../login.defs:
    PASS_MAX_DAYS=90
    PASS_MIN_DAYS=10
    PASS_WARN_AGE=7
    
    save
    
  sudo nano common-auth:
    add "auth required pam_tally2.so deny=5 onerr=fail unlock_time=1800" at the end of the file
    
  Look for hidden and media files
  
  $ ls -la show hidden files
  $ locate *.NAME to find file
  
  $ sudo install bum
  
  $ sudo bum:
    look for bad files
    
  look for shadow permissions
  https://askubuntu.com/questions/445361/what-is-difference-between-etc-shadow-and-etc-passwd
  make sure shadow file and PassWD isnt allowed by user, gest user
  
  disable guest:
    sudo nano /etc/lightdm/lightdm.conf/ or /etc/lightdm/users.conf if no lightdm.conf
    add "allow-guest=false" at the end of the file
    
  
  
  
  
